# “小荷”智能医生问答助手（MedRAG）

---

## 一、项目概述

### 1.1 项目名称
**MedRAG — 基于检索增强生成的智能医疗问答助手**

### 1.2 项目背景
医疗健康领域对知识准确性和可追溯性要求极高，是 RAG 技术最具价值也最具挑战的应用场景之一。大语言模型在医疗领域存在突出的"幻觉"风险——可能编造不存在的药品、错误的剂量或不准确的治疗方案，这在医疗场景下可能造成严重后果。本项目要求学生构建一个面向 **基层医疗机构/在线问诊平台** 的 RAG 智能医疗问答系统，在严格的知识约束下，实现可信赖的医学知识问答。

### 1.3 项目目标

| 维度 | 目标描述 |
|------|---------|
| **知识掌握** | 掌握 RAG 全链路技术：文档解析、多模态处理、混合检索、知识图谱、答案生成与验证 |
| **领域理解** | 理解医疗数据的特殊性（结构化与非结构化混合、多源异构、强专业性） |
| **安全合规** | 掌握医疗场景下的幻觉控制、免责声明、引用溯源等安全保障机制 |
| **工程实践** | 独立完成从医疗文档处理到可运行问答系统的全流程 |
| **创新探索** | 鼓励探索多模态 RAG（医学影像理解）、Agentic RAG 等前沿方向 |

### 1.4 技术栈建议

**（以下技术栈为推荐工具和框架，你可以自行选择）**

| 类别 | 推荐工具/框架 |
|------|-------------|
| 编程语言 | Python 3.10+ |
| LLM 框架 | LangChain / LlamaIndex |
| 向量数据库 | Milvus / Chroma / Weaviate / FAISS |
| 图数据库 | Neo4j（用于疾病-症状-药品知识图谱） |
| 文档解析 | PyMuPDF / pdfplumber / Unstructured / PaddleOCR / Nougat（学术论文） |
| 医学图像处理 | pydicom（DICOM）/ Pillow / OpenCV（医学影像预处理） |
| Embedding 模型 | BGE / M3E / Qwen-Embedding / OpenAI text-embedding-3（自行选择） |
| 重排序模型 | BGE-Reranker / Cohere Rerank / 医学领域微调 Reranker |
| 后端框架 | FastAPI |
| 前端框架 | Streamlit / Gradio |

---

## 二、应用场景描述

### 2.1 场景定义
构建一个面向 **基层医疗机构和在线问诊平台** 的智能医疗问答助手。系统服务于基层医生和患者，基于临床指南、药品说明书、医学文献、疾病知识库等权威资料，提供准确、有循证依据的医学问答服务。

### 2.2 核心用户角色

| 角色 | 需求 |
|------|------|
| **基层医生** | 查询疾病诊疗方案、用药指导、药物相互作用、最新临床指南推荐 |
| **患者/普通用户** | 了解疾病症状、用药注意事项、检查项目含义、日常护理建议 |
| **药师** | 查询药物相互作用、配伍禁忌、不良反应、特殊人群用药调整 |
| **医学生** | 学习疾病知识、理解诊疗流程、复习临床指南要点 |

### 2.3 示例问答场景

#### 场景一：临床诊疗咨询

```
用户提问（基层医生）：
"患者，女，45岁，确诊2型糖尿病5年，目前二甲双胍1000mg bid
控制不佳，HbA1c 8.2%，eGFR 55ml/min，下一步如何调整用药？"

期望系统回答：
根据《中国2型糖尿病防治指南（2024年版）》推荐：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 病情分析：
• 二甲双胍单药治疗血糖控制不佳（HbA1c目标 <7.0%）
• eGFR 55ml/min（CKD 3a期），需注意药物选择

💊 治疗建议（基于指南推荐等级）：
1. 首选联合方案：二甲双胍 + SGLT2抑制剂（如达格列净10mg qd）
   ↳ 推荐理由：SGLT2i具有肾脏保护作用，eGFR>45可用
   ↳ 证据等级：A级推荐，IIa类证据
   
2. 替代方案：二甲双胍 + GLP-1受体激动剂（如利拉鲁肽）
   ↳ 适用于需加强血糖控制且有心血管获益需求的患者

⚠️ 注意事项：
• eGFR 45-59 区间：避免使用二甲双胍+磺脲类组合（低血糖风险↑）
• 建议3个月后复查 HbA1c、肾功能
• 本建议仅供参考，请结合患者具体情况综合判断

📎 参考来源：
  [1] 《中国2型糖尿病防治指南（2024年版）》第7章 药物治疗
  [2] KDIGO 2024 Clinical Practice Guideline for Diabetes Management
  [3] 达格列净说明书 - 适应症与用法用量
```

#### 场景二：药物相互作用查询

```
用户提问：
"患者同时服用华法林和阿司匹林，有什么风险？"

期望系统回答：
⚠️ 【重要药物相互作用】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
华法林 × 阿司匹林：严重相互作用

🔴 风险等级：严重（Major）
📈 相互作用机制：
• 华法林（抗凝药）+ 阿司匹林（抗血小板药）→ 出血风险显著增加
• 两药均影响凝血机制，联用可导致严重出血事件

📊 临床证据：
• 联用后消化道出血风险增加约2-4倍
• 颅内出血风险亦有显著增加

📋 临床建议：
• 除非有明确适应证（如机械瓣膜置换术后），应避免联用
• 如必须联用：密切监测INR，维持INR 2.0-2.5（偏低目标）
• 加用质子泵抑制剂（PPI）保护胃黏膜
• 监测出血体征：牙龈出血、皮肤瘀斑、黑便等

📎 参考来源：
  [1] 药品相互作用数据库 - 华法林相互作用项
  [2] 《抗栓治疗出血风险管理和处理专家共识》
  [3] 华法林钠片说明书 - 药物相互作用
```

---

## 三、系统架构设计

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                        用户交互层（Frontend）                         │
│               Streamlit / Gradio / 微信小程序 / Web                  │
└──────────────────────────────────┬──────────────────────────────────┘
                                   │
┌──────────────────────────────────▼──────────────────────────────────┐
│                       应用服务层（API Gateway）                       │
│                    FastAPI REST API + 鉴权 + 限流                    │
└──────────────────────────────────┬──────────────────────────────────┘
                                   │
┌──────────────────────────────────▼──────────────────────────────────┐
│                       医疗 RAG 核心引擎层                             │
│                                                                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌───────────┐ │
│  │ 医学文档解析  │  │ 多模态索引   │  │  多路检索    │  │ 安全生成   │ │
│  │ & 结构化提取 │─▶│  构建与存储   │─▶│  & 重排序    │─▶│ & 审核模块 │ │
│  └─────────────┘  └─────────────┘  └─────────────┘  └───────────┘ │
│        │                │                │                │         │
│  ┌─────▼─────┐   ┌─────▼─────┐   ┌─────▼─────┐   ┌─────▼──────┐ │
│  │指南PDF解析 │   │向量索引    │   │BM25召回   │   │上下文压缩   │ │
│  │药品说明书  │   │BM25索引   │   │向量召回   │   │幻觉检测    │ │
│  │医学影像处理│   │图谱索引   │   │图谱召回   │   │安全审核    │ │
│  │表格/图表   │   │全文索引   │   │融合策略   │   │引用溯源    │ │
│  │术语标准化  │   │时序索引   │   │重排序    │   │免责声明    │ │
│  └───────────┘   └───────────┘   └───────────┘   └────────────┘ │
└──────────────────────────────────┬──────────────────────────────────┘
                                   │
┌──────────────────────────────────▼──────────────────────────────────┐
│                         数据存储层                                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐   │
│  │ 向量数据库 │  │ 图数据库  │  │ 关系数据库 │  │ 医学影像文件存储  │   │
│  │ (Milvus) │  │ (Neo4j)  │  │ (MySQL)  │  │ (MinIO/OSS)      │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 四、功能模块详细设计

---

### 模块一：医学文档解析与处理（核心难点）

#### 4.1.1 需求描述

医学知识库包含多种高度专业化的文档类型，每类文档有不同的解析难点和安全要求。

#### 4.1.2 支持文档格式

| 格式 | 来源 | 处理难点 |
|------|------|---------|
| PDF | 临床指南、药品说明书、医学教材 | 多栏排版、含药物剂量表/流程图/解剖图 |
| Word（.docx） | 病历模板、会诊报告、诊疗规范 | 含嵌套表格、嵌入式图片、修订痕迹 |
| HTML | 医学百科、药品数据库、诊疗平台 | 动态加载内容、复杂表格、多层嵌套 |
| TXT/Markdown | 医学笔记、FAQ、诊疗流程文档 | 编码问题、缺乏结构化标记 |
| Excel（.xlsx） | 药品目录、检验参考值表、医保目录 | 多Sheet、条件格式、合并单元格、公式 |
| PubMed XML | PubMed 文献摘要数据集 | XML 解析、MeSH 术语提取 |
| 医学影像 | X光片、CT、超声图片（可选） | DICOM 格式解析、图像预处理 |

#### 4.1.3 PDF 文档精细化处理（重点实现）

##### A. 药物剂量表格提取与处理

```
┌──────────────────────────────────────────────────────────────────┐
│              医学文档表格处理流程                                   │
│                                                                  │
│  医学PDF ──▶ 表格区域检测 ──▶ 表格类型分类                         │
│     │              │                │                             │
│     │              │    ┌───────────┼───────────┐                │
│     │              │    ▼           ▼           ▼                │
│     │              │  药物剂量表  检验参考值表  疾病分期表           │
│     │              │    │           │           │                 │
│     │              │    ▼           ▼           ▼                │
│     │              │  ┌────────────────────────────────┐         │
│     │              │  │  结构化提取 + 语义标注          │         │
│     │              │  │  • 药物名称标准化（INN/通用名）  │         │
│     │              │  │  • 剂量单位统一（mg/g/ml/U）   │         │
│     │              │  │  • 特殊人群标注（肝/肾功能调整）│         │
│     │              │  └───────────────┬────────────────┘         │
│     │              │                  │                           │
│     │              │                  ▼                           │
│     │              │  ┌────────────────────────────────┐         │
│     │              │  │  表格摘要 + 关键信息提取        │         │
│     │              │  │  （LLM辅助：提炼临床要点）      │         │
│     │              │  └───────────────┬────────────────┘         │
│     │              │                  │                           │
│     ▼              ▼                  ▼                           │
│  结构化表格数据 + 文本化摘要 + 元数据标注                          │
│  → 向量存储时标注"表格"类型及关联文档段落                           │
│  → 保留原始结构用于前端展示（可交互表格）                           │
└──────────────────────────────────────────────────────────────────┘
```

**要求：**
- 使用 `pdfplumber` / `Camelot` 提取 PDF 中的表格（也可自行检索使用效果更好的表格内容提取工具）
- 针对医学表格实现特殊处理：
  - 药物剂量表：提取药名、剂量、频次、给药途径、特殊人群调整
  - 检验参考值表：提取项目名、参考范围、单位、影响因素
  - 疾病分期表：提取分期标准、各期特征、对应治疗方案
- 对医学术语进行标准化处理（如："对乙酰氨基酚" = "扑热息痛" = "Acetaminophen"）
- 表格数据必须保留来源页码和所属文档章节

##### B. 医学图像/图表处理

```
┌──────────────────────────────────────────────────────────────────┐
│               医学图像/图表处理流程                                │
│                                                                  │
│  医学文档页面                                                    │
│    │                                                             │
│    ▼                                                             │
│  ┌──────────────────┐                                           │
│  │ 图像区域检测与分类 │                                           │
│  │ (规则 + 模型)     │                                           │
│  └────────┬─────────┘                                           │
│           │                                                      │
│    ┌──────┼──────────┬──────────────┐                           │
│    ▼      ▼          ▼              ▼                            │
│  ┌────┐ ┌──────┐ ┌────────┐ ┌──────────┐                      │
│  │解剖│ │流程图│ │数据图表│ │医学影像   │                      │
│  │图谱│ │算法图│ │统计图  │ │X光/CT等  │                      │
│  └──┬─┘ └──┬───┘ └───┬────┘ └────┬─────┘                      │
│     │      │         │           │                               │
│     ▼      ▼         ▼           ▼                               │
│  ┌──────────────────────────────────────────┐                   │
│  │           多模态处理引擎                   │                   │
│  │                                          │                   │
│  │  解剖图：OCR + 多模态LLM描述              │                   │
│  │  流程图：结构化提取（诊疗路径→节点序列）     │                   │
│  │  统计图：数据提取 + LLM文字描述            │                   │
│  │  医学影像：基本信息提取 + 可选AI辅助分析    │                   │
│  └──────────────────┬───────────────────────┘                   │
│                     │                                            │
│                     ▼                                            │
│  ┌──────────────────────────────────────────┐                   │
│  │  输出：图像描述文本 + OCR内容 + 元数据     │                   │
│  │  → 关联所属页面/章节上下文                 │                   │
│  │  → 存入知识库作为可检索内容                │                   │
│  └──────────────────────────────────────────┘                   │
└──────────────────────────────────────────────────────────────────┘
```

**要求：**
- 使用 PyMuPDF 提取 PDF 中嵌入的图像
- 图像分类：区分解剖图、流程图、数据图表、医学影像
- 对流程图（如诊疗路径图）进行结构化提取，转化为步骤序列
- 使用 PaddleOCR 对图像中文字进行 OCR 识别
- 使用多模态 LLM（如 GPT-4V / Qwen-VL）对图像生成医学描述
- 医学影像（可选进阶）：提取影像基本信息（拍摄部位、体位等）

##### C. 临床指南文档层级解析

```python
# 示例：临床指南层级结构解析
class ClinicalGuidelineParser:
    """
    临床指南文档层级结构解析器
    解析出 指南 > 章节 > 推荐意见 > 证据等级 的层级关系
    """
    def parse(self, document) -> ClinicalGuideline:
        return {
            "title": "中国2型糖尿病防治指南（2024年版）",
            "issuing_body": "中华医学会糖尿病学分会",
            "publish_date": "2024-03-15",
            "hierarchy": [
                {
                    "level": "chapter",
                    "number": "第七章",
                    "title": "2型糖尿病的药物治疗",
                    "sections": [
                        {
                            "level": "section",
                            "title": "7.2 二甲双胍",
                            "recommendations": [
                                {
                                    "id": "REC-7-2-1",
                                    "content": "二甲双胍是2型糖尿病一线首选药物...",
                                    "evidence_level": "A",
                                    "recommendation_class": "I",
                                    "key_points": [
                                        "起始剂量500mg bid，逐渐加量至1000mg bid",
                                        "eGFR<45时禁用",
                                        "主要不良反应为胃肠道反应"
                                    ]
                                }
                            ]
                        }
                    ]
                }
            ],
            "tables": [...],         # 药物剂量表等
            "figures": [...],        # 诊疗流程图等
            "abbreviations": {       # 缩略语表
                "HbA1c": "糖化血红蛋白",
                "eGFR": "估算肾小球滤过率",
                "SGLT2i": "钠-葡萄糖共转运蛋白2抑制剂"
            },
            "metadata": {
                "source": "中华糖尿病杂志",
                "doi": "10.3760/...",
                "guideline_type": "临床实践指南"
            }
        }
```

#### 4.1.4 医学术语标准化

```python
# 医学术语标准化处理
class MedicalTermNormalizer:
    """
    医学术语标准化
    将各种别名/缩写/商品名统一为标准术语
    """
    def normalize(self, term: str) -> NormalizedTerm:
        """
        示例映射：
        - "感冒" → "上呼吸道感染" (ICD-10: J06.9)
        - "扑热息痛" → "对乙酰氨基酚" (ATC: N02BE01)
        - "DM2" → "2型糖尿病" (ICD-10: E11)
        - "ACEI" → "血管紧张素转换酶抑制剂"
        - "高血压3级" → "3级高血压" (ICD-10: I10)
        """
        return {
            "original": term,
            "normalized": "对乙酰氨基酚",
            "synonyms": ["扑热息痛", "acetaminophen", "paracetamol"],
            "icd10": None,           # 疾病类术语才有
            "atc_code": "N02BE01",   # 药物类术语才有
            "snomed_ct": "387517004"
        }
```

**要求：**
- 建立医学同义词词典（不少于 500 组映射关系），覆盖：
  - 常见疾病的俗称与标准名
  - 药品通用名、商品名、化学名
  - 常见医学缩写
- 实现基于词典的术语标准化
- 可选进阶：接入 UMLS / SNOMED CT 术语服务

#### 4.1.5 文档切分策略（Chunking）

| 策略 | 说明 | 适用场景 |
|------|------|---------|
| **指南切分** | 按推荐意见粒度切分，每条推荐含证据等级 | 临床指南 |
| **药品切分** | 按药品维度切分，每种药品信息独立成 chunk | 药品说明书 |
| **语义切分** | 按段落/语义边界切分 | 医学文献摘要 |
| **QA对切分** | 构建问答对作为 chunk，天然匹配检索场景 | 医学 FAQ |
| **父子切分** | 小块检索，返回时扩展为父块（如整章内容） | 医学教材 |
| **表格独立切分** | 表格单独成 chunk，附加上下文摘要 | 含表格的文档 |
| **窗口滑切分** | 固定 token 数 + 重叠窗口 + 句边界对齐 | 通用医学文本 |

**要求：**
- 临床指南使用 **指南切分**（按推荐意见粒度，保留证据等级和推荐类别）
- 药品说明书使用 **药品切分**（每种药品独立 chunk，含完整用药信息）
- 每个 chunk 必须保留元数据：
  - `source`：来源文档
  - `doc_type`：临床指南 / 药品说明书 / 医学文献 / 疾病知识 / FAQ
  - `evidence_level`：证据等级（指南推荐）
  - `medical_field`：科室（内科/外科/妇产科/儿科/...）
  - `drug_name`：涉及药品名称（药品说明书 chunk）
  - `disease_name`：涉及疾病名称
  - `page_number`：页码
  - `publish_date`：发布日期（用于时效性过滤）
- 实现 **父子 chunk** 机制：按段落小块检索，返回时扩展上下文
- 药物信息 chunk 需要特殊标记，检索时优先展示

#### 4.1.6 交付物
- [ ] `parsers/` 目录：各格式文档解析器
- [ ] `pdf_processor.py`：PDF 精细化处理器（含药物表格/医学图表/图像）
- [ ] `term_normalizer.py`：医学术语标准化工具
- [ ] `chunker.py`：多种切分策略实现（含指南切分、药品切分）
- [ ] `data_pipeline.py`：端到端医学文档处理流水线
- [ ] `medical_synonym_dict.json`：医学同义词词典（≥500组）
- [ ] 单元测试覆盖核心解析逻辑

---

### 模块二：知识库构建与索引

#### 4.2.1 需求描述

将处理后的医学文档构建为多种索引。医疗场景需要特别注意**知识时效性**（药品说明书更新、指南修订）和**来源可信度**（权威指南 vs 普通科普）。

#### 4.2.2 向量索引构建

```
医学文档Chunk ──▶ Embedding模型 ──▶ 向量 ──▶ 向量数据库存储
                      │
              ┌───────┴────────────────────┐
              │      模型选择对比           │
              │                           │
              │  BGE-large-zh              │ ← 通用中文
              │  PubMedBERT (中文微调版)    │ ← 医学领域推荐
              │  M3E-large                 │ ← 中文通用
              │  text2vec-large-chinese    │ ← 对比基线
              │  OpenAI text-embedding-3   │ ← 多语言支持
              └───────────────────────────┘
```

**要求：**
- 至少对比 3 种 Embedding 模型在医学知识领域的检索效果
- 特别关注医学专业术语和药品名称的检索准确性
- 向量数据库使用 Milvus 或 Chroma
- 元数据字段设计（metadata filtering 用）：
  - `source`：来源文档
  - `doc_type`：临床指南 / 药品说明书 / 文献 / 疾病知识 / FAQ
  - `medical_field`：科室（内科/外科/妇产科/儿科/皮肤科/...）
  - `evidence_level`：证据等级（A/B/C/D）
  - `drug_name`：药品名称列表
  - `disease_icd10`：疾病 ICD-10 编码
  - `publish_date`：发布日期
  - `last_review_date`：最近审核日期
  - `credibility_score`：来源可信度评分
- 实现时效性过滤：优先检索最新版本的指南和说明书
- 实现来源可信度加权：高可信度来源的 chunk 在检索时获得权重加成

#### 4.2.3 全文索引构建（BM25）

**要求：**
- 使用 `rank_bm25` 或 Elasticsearch 构建 BM25 全文索引
- 针对中文医学文本进行分词优化：
  - 使用 `jieba` 分词 + **医学领域自定义词典**（包含药品名、疾病名、医学术语）
  - 保证医学专有名词不被错误切分：
    - ✅ "糖化血红蛋白" 不拆分
    - ✅ "对乙酰氨基酚" 不拆分
    - ✅ "血管紧张素转换酶抑制剂" 不拆分
    - ❌ 避免 "阿卡波糖" 被切为 "阿卡 + 波糖"
- 实现医学停用词过滤（去除常见但无检索价值的词汇）
- 支持药品名、疾病名的精确匹配检索

#### 4.2.4 医学知识图谱构建（GraphRAG 部分）

```
┌──────────────────────────────────────────────────────────────────────┐
│                   医学知识图谱构建流程                                 │
│                                                                      │
│  医学文档（指南、说明书、教科书）                                       │
│    │                                                                 │
│    ▼                                                                 │
│  ┌──────────────────┐       ┌─────────────────────┐                 │
│  │ 医学实体抽取(NER) │──────▶│      实体列表        │                 │
│  │                  │       │                     │                 │
│  │  • 疾病名称       │       │  疾病：2型糖尿病     │                 │
│  │  • 药品名称       │       │  药品：二甲双胍      │                 │
│  │  • 症状/体征      │       │  症状：多饮、多尿    │                 │
│  │  • 检查项目       │       │  检查：HbA1c        │                 │
│  │  • 科室          │       │  科室：内分泌科      │                 │
│  │  • 不良反应       │       │  不良反应：乳酸酸中毒│                 │
│  │  • 禁忌证        │       │  禁忌证：eGFR<30    │                 │
│  └──────────────────┘       └─────────┬───────────┘                 │
│                                       │                              │
│                                       ▼                              │
│  ┌──────────────────┐       ┌─────────────────────┐                 │
│  │ 医学关系抽取(RE)  │──────▶│    关系三元组         │                 │
│  │                  │       │                     │                 │
│  │  • 疾病-症状     │       │  (糖尿病, 典型症状,  │                 │
│  │  • 疾病-药品     │       │   多饮多尿多食)      │                 │
│  │  • 药品-适应证   │       │  (二甲双胍, 适应证,  │                 │
│  │  • 药品-不良反应 │       │   2型糖尿病)         │                 │
│  │  • 药品-禁忌证   │       │  (二甲双胍, 禁忌证,  │                 │
│  │  • 药品-相互作用 │       │   eGFR<30)          │                 │
│  │  • 疾病-检查     │       │  (华法林, 相互作用,  │                 │
│  │  • 症状-科室     │       │   阿司匹林)          │                 │
│  │  • 指南-推荐     │       │  (糖尿病, 推荐检查,  │                 │
│  └──────────────────┘       │   HbA1c)            │                 │
│                             └─────────┬───────────┘                 │
│                                       │                              │
│                                       ▼                              │
│  ┌──────────────────────────────────────────────────┐               │
│  │              Neo4j 图数据库存储                    │               │
│  │                                                  │               │
│  │  节点类型：                                        │               │
│  │    (:Disease {name, icd10, description})          │               │
│  │    (:Drug {name, atc_code, dosage_form})          │               │
│  │    (:Symptom {name, description})                  │               │
│  │    (:Examination {name, normal_range})             │               │
│  │    (:Guideline {title, publish_date})              │               │
│  │    (:Recommendation {content, evidence_level})     │               │
│  │                                                  │               │
│  │  关系类型：                                        │               │
│  │    (:Disease)-[:HAS_SYMPTOM]->(:Symptom)          │               │
│  │    (:Drug)-[:INDICATED_FOR]->(:Disease)           │               │
│  │    (:Drug)-[:CONTRAINDICATED_IN]->(:Condition)    │               │
│  │    (:Drug)-[:INTERACTS_WITH]->(:Drug)             │               │
│  │    (:Drug)-[:CAUSES_ADR]->(:AdverseReaction)      │               │
│  │    (:Guideline)-[:RECOMMENDS]->(:Recommendation)  │               │
│  │    (:Disease)-[:RECOMMENDED_EXAM]->(:Examination) │               │
│  └──────────────────────────────────────────────────┘               │
└──────────────────────────────────────────────────────────────────────┘
```

**要求：**
- 使用 LLM 辅助进行医学实体和关系的抽取（可结合规则引擎）
- 实体标准化：抽取的实体映射到 ICD-10 / ATC 编码体系
- 将抽取结果存入 Neo4j 图数据库
- 实现基于图的多种检索模式：
  - **疾病-药品路径**：给定疾病，查询推荐用药（沿 INDICATED_FOR 关系）
  - **药品相互作用路径**：给定药品A，查询可能相互作用的药品B
  - **症状-疾病路径**：给定症状，查询可能关联的疾病
  - **多跳查询**：症状 → 疾病 → 推荐用药 → 注意事项
- 将图检索结果转化为文本上下文，作为 RAG 的补充信息来源

---

### 模块三：多路检索与重排序

#### 4.3.1 多路召回架构

```
用户医学查询
    │
    ├──▶ 查询预处理
    │      │
    │      ├── 医学术语标准化（"感冒"→"上呼吸道感染"）
    │      ├── 关键词/实体提取（疾病名、药品名、症状）
    │      ├── 查询分类（诊疗咨询/药品查询/相互作用/症状评估）
    │      ├── 查询扩展（同义词扩展："头痛"→"头疼/头部疼痛/cephalgia"）
    │      └── HyDE（生成假设性医学答案文档，用于向量检索）
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│                      多路召回层                               │
│                                                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌───────────┐  │
│  │  BM25    │  │  向量     │  │  Graph   │  │  FAQ匹配   │  │
│  │  关键词   │  │  语义     │  │  图谱    │  │  精确匹配   │  │
│  │  召回    │  │  召回     │  │  召回    │  │  召回      │  │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └─────┬─────┘  │
│       │             │             │               │          │
│       └─────────────┼─────────────┼───────────────┘          │
│                     │             │                          │
│                     ▼             ▼                          │
│            ┌──────────────────────────┐                     │
│            │    结果融合（RRF/加权）    │                     │
│            │  + 时效性加权             │                     │
│            │  + 来源可信度加权         │                     │
│            └────────────┬─────────────┘                     │
│                         │                                    │
│                         ▼                                    │
│            ┌──────────────────────────┐                     │
│            │   医学领域重排序          │                     │
│            │  (Cross-Encoder +        │                     │
│            │   医学相关性约束)         │                     │
│            └────────────┬─────────────┘                     │
│                         │                                    │
│                         ▼                                    │
│            ┌──────────────────────────┐                     │
│            │   安全性过滤              │                     │
│            │  (去除过期/低可信度内容)  │                     │
│            └────────────┬─────────────┘                     │
│                         │                                    │
│                         ▼                                    │
│              Top-K 最终结果                                  │
└─────────────────────────────────────────────────────────────┘
```

#### 4.3.2 各召回路径详细设计

##### 路径一：BM25 关键词召回

```python
class MedicalBM25Retriever:
    def __init__(self):
        self.tokenizer = JiebaTokenizer(custom_dict="medical_dict.txt")
        self.term_normalizer = MedicalTermNormalizer()

    def retrieve(self, query: str, top_k: int = 20) -> List[Document]:
        # 1. 医学术语标准化
        normalized_query = self.term_normalizer.normalize_text(query)

        # 2. 中文医学分词
        tokens = self.tokenizer.tokenize(normalized_query)

        # 3. 提取关键医学实体（药品名、疾病名等）
        medical_entities = self.extract_medical_entities(query)

        # 4. BM25 检索（实体匹配加权）
        scores = self.bm25.get_scores(tokens)

        # 5. 对包含关键实体的文档加分
        for doc in self.documents:
            if any(entity in doc.content for entity in medical_entities):
                scores[doc.index] *= 1.5  # 实体匹配加权

        top_indices = np.argsort(scores)[-top_k:]
        return [self.documents[i] for i in top_indices]
```

**要求：**
- 实现医学术语标准化后的 BM25 检索（解决同义词匹配问题）
- 医学领域自定义分词词典不少于 1000 个术语
- 对包含关键医学实体（药品名、疾病名）的文档给予检索加权
- 支持精确匹配模式（如精确查询某个药品名）

##### 路径二：向量语义召回

```python
class MedicalVectorRetriever:
    def __init__(self):
        self.embedder = MedicalEmbedder("BAAI/bge-large-zh-v1.5")
        self.collection = milvus_client.get_collection("medical_docs")

    def retrieve(self, query: str, top_k: int = 20,
                 filters: dict = None) -> List[Document]:
        # 1. 查询向量化
        query_embedding = self.embedder.encode(query)

        # 2. 构建过滤条件
        filter_expr = self._build_filter(filters)
        # 示例："medical_field == '内分泌科' AND evidence_level in ['A', 'B']"

        # 3. 向量检索（支持元数据过滤）
        results = self.collection.search(
            data=[query_embedding],
            anns_field="embedding",
            param={"metric_type": "COSINE", "params": {"nprobe": 16}},
            limit=top_k,
            filter=filter_expr,
            output_fields=["content", "source", "doc_type",
                          "evidence_level", "publish_date"]
        )

        return results

    def _build_filter(self, filters: dict) -> str:
        """根据用户意图构建元数据过滤表达式"""
        conditions = []
        if filters.get("doc_type"):
            conditions.append(f"doc_type == '{filters['doc_type']}'")
        if filters.get("medical_field"):
            conditions.append(f"medical_field == '{filters['medical_field']}'")
        if filters.get("min_evidence"):
            conditions.append(
                f"evidence_level in {filters['min_evidence']}")
        return " and ".join(conditions) if conditions else ""
```

**要求：**
- 实现余弦相似度检索
- 支持多维元数据过滤（按科室、文档类型、证据等级等）
- 实现时效性排序：同等相关性下，优先返回较新的文档
- 对比不同 Embedding 模型（通用模型 vs 医学领域模型）的检索效果

##### 路径三：GraphRAG 图谱召回

```python
class MedicalGraphRAGRetriever:
    def __init__(self):
        self.neo4j = Neo4jClient()
        self.llm = ChatModel()
        self.entity_extractor = MedicalEntityExtractor()

    def retrieve(self, query: str, query_type: str = "auto") -> List[Document]:
        # 1. 从查询中提取医学实体
        entities = self.entity_extractor.extract(query)

        # 2. 根据查询类型选择图检索策略
        if query_type == "auto":
            query_type = self._classify_query(query)

        # 3. 执行图检索
        if query_type == "drug_interaction":
            # 药物相互作用查询
            subgraph = self._query_drug_interaction(entities["drugs"])
        elif query_type == "treatment":
            # 治疗方案查询
            subgraph = self._query_treatment(entities["diseases"])
        elif query_type == "symptom_disease":
            # 症状-疾病关联查询
            subgraph = self._query_symptom_disease(entities["symptoms"])
        else:
            # 通用多跳检索
            subgraph = self._query_general(entities, hops=2)

        # 4. 将子图转化为文本描述
        context = self._graph_to_medical_text(subgraph)

        # 5. LLM 辅助生成结构化摘要
        summary = self.llm.generate(
            f"基于以下医学知识图谱信息，提炼关键要点：\n{context}\n问题：{query}"
        )

        return [Document(content=context + summary, source="graph",
                        metadata={"graph_entities": entities})]

    def _query_drug_interaction(self, drugs: List[str]) -> SubGraph:
        """查询药物相互作用"""
        return self.neo4j.query("""
            MATCH (d1:Drug)-[r:INTERACTS_WITH]->(d2:Drug)
            WHERE d1.name IN $drugs OR d2.name IN $drugs
            OPTIONAL MATCH (r)-[:HAS_EVIDENCE]->(e:Evidence)
            RETURN d1, r, d2, e
        """, drugs=drugs)
```

**要求：**
- 实现查询意图分类（药物相互作用/治疗方案/症状评估/检查解读）
- 根据不同查询意图选择不同的图检索策略
- 实现多跳图查询（1-2 跳）
- 实现医学子图到文本的转化（结构化的医学描述）
- 对比有无 GraphRAG 对复杂医学问题（尤其是药物相互作用查询）的回答质量

##### 路径四：FAQ 精确匹配召回（补充路径）

```python
class MedicalFAQRetriever:
    """
    医学FAQ精确匹配召回
    针对高频问题的预构建问答对
    """
    def retrieve(self, query: str, top_k: int = 5) -> List[Document]:
        # 1. 语义相似度匹配（与预构建的FAQ问题库比较）
        query_embedding = self.embedder.encode(query)
        similar_faqs = self.faq_collection.search(query_embedding, top_k=10)

        # 2. 过滤低相似度结果
        filtered = [f for f in similar_faqs if f.score > 0.85]

        # 3. 返回匹配的FAQ答案
        return [Document(content=faq.answer, source=faq.source,
                        metadata={"match_type": "faq_exact"})
                for faq in filtered]
```

**要求：**
- 预构建不少于 50 个高频医学问答对
- 覆盖常见问题类别：
  - 常见疾病基础知识（"糖尿病有哪些症状？"）
  - 常见药品用药指导（"二甲双胍怎么吃？"）
  - 检查指标解读（"血糖正常值是多少？"）
  - 药物相互作用（"XX药和YY药能一起吃吗？"）
- FAQ 召回的结果具有高可信度，应优先使用

#### 4.3.3 查询理解与改写（Query Transformation）

| 技术 | 说明 | 医学场景实现要求 |
|------|------|----------------|
| **术语标准化** | 将口语化/缩写转为标准医学术语 | "感冒"→"上呼吸道感染"；"降压药"→"抗高血压药物" |
| **查询扩展** | 同义词、近义词扩展 | "头痛"→"头疼 / 头部疼痛 / cephalalgia" |
| **查询分类** | 判断查询意图类型 | 诊疗咨询 / 药品查询 / 相互作用 / 检查解读 / 症状评估 |
| **HyDE** | 生成假设性医学答案，用于向量检索 | 对专业术语密集的查询特别有效 |
| **查询分解** | 将复杂问题分解为子问题 | "糖尿病合并高血压如何用药"→"糖尿病用药"+"高血压用药"+"药物相互作用" |
| **实体识别** | 提取查询中的疾病名、药品名等 | 用于图谱检索和元数据过滤 |

**要求：**
- 实现基于 LLM 的查询意图分类器
- 实现医学实体识别（NER），提取疾病名、药品名、症状、检查项目
- 实现术语标准化和查询扩展
- 可选：实现 HyDE（在医学场景下需要验证假设文档的质量）

#### 4.3.4 结果融合策略

```python
class MedicalResultFusion:
    def weighted_rrf_fusion(self, result_lists: List[List[Document]],
                            weights: List[float] = None,
                            k: int = 60) -> List[Document]:
        """
        加权 RRF 融合 + 医学特殊加权
        score(d) = Σ weight_i / (k + rank_i(d))
                 × credibility_boost(d)
                 × freshness_boost(d)
        """
        weights = weights or [1.0] * len(result_lists)
        scores = defaultdict(float)

        for i, results in enumerate(result_lists):
            for rank, doc in enumerate(results):
                base_score = weights[i] / (k + rank + 1)

                # 来源可信度加成
                credibility = doc.metadata.get("credibility_score", 0.5)
                credibility_boost = 1.0 + 0.3 * credibility

                # 时效性加成（越新越高）
                pub_date = doc.metadata.get("publish_date", "2020-01-01")
                freshness_boost = self._calc_freshness(pub_date)

                scores[doc.id] += base_score * credibility_boost * freshness_boost

        sorted_docs = sorted(scores.items(), key=lambda x: x[1], reverse=True)
        return [self.get_doc(doc_id) for doc_id, _ in sorted_docs]
```

**要求：**
- 实现 RRF 融合，支持可配置各路召回权重
- 实现来源可信度加权（权威指南 > 普通科普）
- 实现时效性加权（新版指南 > 旧版指南）
- 去重处理：同一文档的不同 chunk 合并，保留最相关的片段
- 药物信息相关结果优先展示

#### 4.3.5 重排序（Reranking）

```python
class MedicalReranker:
    def __init__(self):
        self.reranker = BGEReranker("BAAI/bge-reranker-v2-m3")

    def rerank(self, query: str, documents: List[Document],
               top_k: int = 5) -> List[Document]:
        # 1. Cross-Encoder 计算相关性分数
        pairs = [(query, doc.content) for doc in documents]
        scores = self.reranker.compute_score(pairs)

        # 2. 医学特殊约束加成
        for i, doc in enumerate(documents):
            # 证据等级加成
            evidence = doc.metadata.get("evidence_level", "")
            if evidence == "A":
                scores[i] *= 1.3
            elif evidence == "B":
                scores[i] *= 1.1

            # 药品说明书命中加成（查询药品时）
            if "drug_name" in doc.metadata:
                scores[i] *= 1.15

        # 3. 排序并返回 Top-K
        scored_docs = sorted(zip(documents, scores),
                           key=lambda x: x[1], reverse=True)
        return [doc for doc, _ in scored_docs[:top_k]]
```

**要求：**
- 使用 Cross-Encoder 模型对融合结果重排序
- 实现证据等级加权（A级证据 > B级 > C级）
- 实现来源类型加权（指南/说明书 > 文献 > 科普）
- 对比重排序前后的检索准确率

#### 4.3.6 交付物
- [ ] `retrievers/` 目录：BM25 / 向量 / GraphRAG / FAQ 四种检索器
- [ ] `hybrid_retriever.py`：混合检索器（融合多路结果）
- [ ] `reranker.py`：医学领域重排序模块
- [ ] `query_processor.py`：查询预处理（术语标准化/分类/实体提取）
- [ ] `query_transform.py`：查询改写（扩展/HyDE/分解）
- [ ] `fusion.py`：多路结果融合（含可信度/时效性加权）
- [ ] `evaluation/`：检索效果评估脚本和指标

---

### 模块五：安全生成与后处理

#### 4.4.1 上下文压缩

```
检索到的医学文档片段
    │
    ├── 相关性过滤：去除与问题无关的片段
    ├── 时效性检查：标记过期内容（旧版指南被新版替代）
    ├── 矛盾检测：检测不同来源的矛盾信息，保留更权威/更新的
    ├── 信息压缩：LLM 提取每个片段中与问题相关的核心信息
    ├── 去重合并：合并重复信息，保留最佳来源
    │
    ▼
压缩后的上下文（适合 LLM 上下文窗口，高信息密度）
```

**要求：**
- 实现基于相关性的上下文过滤
- 实现时效性检查（标记已被新版替代的旧内容）
- 实现矛盾检测（不同来源信息冲突时的处理策略）
- 实现 LLM 辅助的上下文压缩

#### 4.4.2 Prompt 工程设计

```python
# 医疗问答系统 Prompt 模板
MEDICAL_QA_PROMPT = """
你是一位严谨、专业的临床医生助手，请基于以下医学参考资料回答用户的医学问题。

## ⚠️ 核心安全规则（必须严格遵守）：
1. 严格基于参考资料回答，绝不编造药品名称、剂量、适应症
2. 所有用药建议必须标注来源文献/指南
3. 如资料中没有相关信息，必须明确声明："根据现有参考资料无法确定，建议查阅最新指南或咨询专科医生"
4. 涉及用药剂量时，必须注明适用人群和特殊人群调整
5. 必须标注证据等级（如有）
6. 在回答末尾添加免责声明

## 回答格式要求：
1. 先进行病情分析/问题解读
2. 给出基于循证医学的回答
3. 列出注意事项和禁忌
4. 标注参考来源 [序号]
5. 添加免责声明

## 参考资料：
{context}

## 用户问题：
{question}

## 回答：
"""
```

**要求：**
- 设计结构化的系统 Prompt，明确安全规则和输出格式
- 实现严格的引用约束（每个医学论断需有来源）
- 实现"无法回答"的安全兜底（避免幻觉造成医疗风险）
- 实现免责声明自动添加
- 可选：实现 Self-RAG（模型自判断检索是否充分）

#### 4.4.3 医学安全审核

```
LLM 生成答案
    │
    ▼
┌──────────────────────────────────────────────┐
│              医学安全审核模块                   │
│                                              │
│  1. 引用验证                                  │
│     → 检查引用的法条/指南/说明书是否真实存在    │
│     → 检查药品名称、剂量是否与原文一致         │
│                                              │
│  2. 幻觉检测                                  │
│     → 检查答案中是否存在参考资料中没有的论断    │
│     → 对每个关键医学论断标记来源               │
│                                              │
│  3. 矛盾检测                                  │
│     → 检查答案内部是否存在逻辑矛盾            │
│     → 检查与常识性医学知识是否冲突            │
│                                              │
│  4. 合规性检查                                │
│     → 确认包含免责声明                        │
│     → 确认未给出超范围的诊断/处方建议          │
│                                              │
│  5. 完整性评估                                │
│     → 答案是否覆盖了问题的所有方面             │
│     → 是否遗漏了重要的安全提示                │
└──────────────────────┬───────────────────────┘
                       │
              ┌────────┴────────┐
              │                 │
          通过 │             不通过│
              ▼                 ▼
         输出答案         拒绝回答/要求补充信息
```

**要求：**
- 实现引用验证（药品名、剂量、指南编号是否与原文一致）
- 实现幻觉检测（标记答案中无来源支持的关键论断）
- 实现免责声明自动添加
- 实现合规性检查（不给出超范围的诊断建议）
- 对高风险回答（涉及用药剂量、禁忌证）进行额外审核

#### 4.4.4 回答后处理

| 处理环节 | 说明 |
|---------|------|
| **引用格式化** | 将引用标准化为统一的参考文献格式 |
| **药品信息高亮** | 对药品名称、剂量、用法进行特殊标注 |
| **警示信息突出** | 禁忌证、不良反应、相互作用信息用⚠️标记 |
| **结构化展示** | 分点列举、表格展示（药物剂量对比表等） |
| **置信度标注** | 对答案的可信度进行评分展示 |
| **免责声明** | 自动添加"本回答仅供参考，不能替代专业医疗建议" |
| **追问建议** | 基于回答生成可能的追问方向（如"您可能还想了解..."） |
| **就医建议** | 必要时建议用户到特定科室就诊 |

#### 4.4.5 交付物
- [ ] `generator.py`：答案生成模块
- [ ] `context_compressor.py`：上下文压缩模块
- [ ] `safety_checker.py`：医学安全审核模块
- [ ] `postprocessor.py`：回答后处理模块
- [ ] `prompts/`：各类 Prompt 模板

---

## 五、RAG 优化专题

### 5.1 优化方向矩阵

| 环节 | 问题 | 优化策略 | 难度 |
|------|------|---------|------|
| 文档解析 | PDF 药物剂量表格提取不准 | 多种工具组合使用 + 人工校验 | ⭐⭐⭐ |
| 文档解析 | 医学图像/图表理解困难 | 多模态 LLM + OCR<br />（MonkeyOCR/其他） | ⭐⭐⭐⭐ |
| 文档解析 | 医学术语不统一 | 术语标准化 + 同义词映射 | ⭐⭐ |
| 切分 | 固定切分破坏指南推荐意见完整性 | 指南专用切分策略 | ⭐⭐ |
| 索引 | 通用 Embedding 不擅长医学术语 | 医学领域微调 Embedding | ⭐⭐⭐⭐ |
| 检索 | 口语化查询与专业文档匹配差 | 术语标准化 + 查询改写 | ⭐⭐⭐ |
| 检索 | 药物相互作用查询需图结构 | GraphRAG | ⭐⭐⭐ |
| 检索 | 单次检索不够充分 | 迭代检索 / 多轮检索 | ⭐⭐⭐ |
| 检索 | 检索结果包含过期/低质内容 | 时效性过滤 + 可信度排序 | ⭐⭐ |
| 生成 | LLM 编造不存在的药品/剂量 | 严格引用约束 + 幻觉检测 | ⭐⭐⭐⭐ |
| 生成 | 长文档超出上下文窗口 | 上下文压缩 / Map-Reduce | ⭐⭐⭐ |
| 端到端 | 回答缺乏循证依据 | 证据等级标注 + Self-RAG | ⭐⭐⭐ |
| 安全 | 高风险医学建议可能出错 | 多轮安全审核 + 免责声明 | ⭐⭐⭐ |

### 5.2 进阶优化技术（可选探索）

#### A. Self-RAG（自反思 RAG）在医学场景

```
用户医学提问 ──▶ LLM 判断：需要检索吗？检索哪类资料？
                    │
               Yes  │  No（纯常识性问题）
               ▼    │  ▼
             检索  │  直接回答 + 标注"非循证回答"
               │
               ▼
             生成初步答案
               │
               ▼
             LLM 自评审：
             - 每个论断是否有引用支持？(IsCited)
             - 药品名称和剂量是否准确？(IsAccurate)
             - 是否遗漏重要禁忌/注意事项？(IsComplete)
             - 是否添加了免责声明？(IsSafe)
               │
          全部通过 │ 有不通过项
               ▼      │
          输出答案   补充检索/修正后重新生成
```

#### B. 多模态医学 RAG（进阶探索）

```
用户提问 + 上传医学影像
    │
    ├── 影像分析模块
    │   ├── 影像预处理（标准化、增强）
    │   ├── 多模态 LLM 影像理解
    │   └── 生成影像描述文本
    │
    ▼
影像描述 + 文本问题 ──▶ 多模态检索
    │
    ├── 文本检索：基于描述检索相关文献/指南
    ├── 影像检索：基于影像特征检索相似病例（可选）
    │
    ▼
融合答案（文本证据 + 影像分析结果）
```

#### C. Agentic RAG（智能体驱动的医学问答）

```
用户医学提问 ──▶ Medical RAG Agent（ReAct模式）
                   │
                   ├── 思考：这是糖尿病用药问题，需要查询最新指南
                   ├── 行动：调用 临床指南检索工具
                   ├── 观察：检索到2024版糖尿病指南相关章节
                   ├── 思考：需要确认患者肾功能对用药的影响
                   ├── 行动：调用 药品说明书检索工具（二甲双胍+肾功能）
                   ├── 观察：获取到二甲双胍的肾功能调整建议
                   ├── 思考：eGFR 55，需要确认是否可以用SGLT2i
                   ├── 行动：调用 知识图谱检索（SGLT2i+肾功能适应证）
                   ├── 观察：获取到达格列净的肾功能使用条件
                   ├── 思考：需要检查是否有药物相互作用
                   ├── 行动：调用 药物相互作用检查工具
                   ├── 观察：二甲双胍+达格列净无严重相互作用
                   ├── 思考：信息充分，可以生成综合建议
                   └── 行动：生成最终答案（含引用和免责声明）
```

---

## 六、评估体系

### 6.1 评估维度

| 维度 | 指标 | 说明 |
|------|------|------|
| **检索质量** | Recall@K | Top-K 结果中包含正确答案的比例 |
| | MRR | 第一个正确结果的排名倒数 |
| | NDCG | 归一化折损累积增益 |
| | 实体命中率 | 查询中的关键医学实体是否被检索结果覆盖 |
| **生成质量** | Faithfulness | 回答是否忠于检索到的内容（医学场景关键指标） |
| | Answer Relevancy | 回答与问题的相关度 |
| | Context Precision | 使用的上下文的精确度 |
| | Hallucination Rate | 幻觉率（医学场景需极低） |
| **安全性** | Citation Accuracy | 引用准确率（引用是否真实存在且正确） |
| | Safety Compliance | 安全合规率（是否包含免责声明、禁忌提示等） |
| | Harmful Advice Rate | 有害建议率（需趋近于0） |
| **端到端** | Answer Correctness | 最终答案的正确率 |
| | Clinical Accuracy | 临床准确性（由医学专业人员评估） |
| | Latency | 端到端响应延迟 |

### 6.2 评估数据集构建要求

**要求：**
- 构建不少于 **150 条** 医学问答测试集
- 每条包含：问题、期望答案、相关文档（ground truth）、问题类型、难度
- 覆盖场景：
  - 常见疾病基础知识（20条）
  - 药物治疗方案咨询（30条）
  - 药物相互作用查询（20条）
  - 检查指标解读（20条）
  - 症状评估与鉴别（20条）
  - 临床指南推荐查询（20条）
  - 特殊人群用药（20条）
  - 药物不良反应（20条）
- 覆盖至少 5 个科室（内分泌科、心内科、呼吸科、消化科、神经内科）
- 每个难度等级：基础查询 / 综合分析 / 复杂多跳推理
- 安全评估专项：包含 20+ 条容易触发幻觉的诱导性问题
- 使用 RAGAS 框架 + 医学专家抽检进行混合评估

### 6.3 评估对比实验要求

| 实验编号 | 对比内容 | 预期产出 |
|---------|---------|---------|
| Exp1 | 单路 BM25 vs 单路向量 vs 混合检索 | 混合检索在医学场景优势明显 |
| Exp2 | 有无术语标准化的对比 | 术语标准化显著提升召回率 |
| Exp3 | 有无 Reranker 的对比 | Reranker 显著提升精确度 |
| Exp4 | 有无 GraphRAG 的对比 | GraphRAG 在药物相互作用查询上效果显著 |
| Exp5 | 不同 Chunking 策略对比 | 指南专用切分 > 通用切分 |
| Exp6 | 不同 Embedding 模型对比 | 选出医学领域最优模型 |
| Exp7 | 通用 Prompt vs 安全增强 Prompt | 安全增强显著降低幻觉率 |
| Exp8 | 基础 RAG vs Self-RAG | Self-RAG 减少幻觉、提升完整性 |

---

## 七、项目交付要求

### 7.1 代码规范

- 项目结构清晰，遵循 Python 项目规范（src layout）
- 关键模块有完整的 docstring 和类型注解
- 核心功能有单元测试（覆盖率 ≥ 60%）
- 配置文件与代码分离（使用 YAML / TOML 配置）
- 提供 `requirements.txt` 或 `pyproject.toml` 依赖管理
- 医学同义词词典等数据文件与代码分离

### 7.2 项目目录结构（建议）

```
med-rag/
├── README.md                         # 项目说明
├── requirements.txt                  # 依赖列表
├── pyproject.toml                    # 项目配置
├── config/
│   ├── config.yaml                   # 全局配置
│   └── medical_dict.txt              # 医学领域分词词典
├── data/
│   ├── raw/                          # 原始医学文档
│   │   ├── guidelines/               # 临床指南
│   │   ├── drug_labels/              # 药品说明书
│   │   ├── literature/               # 医学文献
│   │   └── textbooks/                # 医学教材
│   ├── processed/                    # 处理后的文档
│   ├── knowledge_graph/              # 知识图谱数据
│   ├── medical_synonyms.json         # 医学同义词词典
│   └── evaluation/                   # 评估数据集
├── src/
│   ├── __init__.py
│   ├── parsers/                      # 文档解析模块
│   │   ├── __init__.py
│   │   ├── pdf_parser.py             # PDF 解析器
│   │   ├── drug_label_parser.py      # 药品说明书解析器
│   │   ├── guideline_parser.py       # 临床指南解析器
│   │   ├── medical_table_extractor.py # 医学表格提取
│   │   └── medical_image_processor.py # 医学图像/图表处理
│   ├── terminology/                  # 术语标准化模块
│   │   ├── __init__.py
│   │   ├── normalizer.py             # 术语标准化器
│   │   ├── entity_extractor.py       # 医学实体抽取
│   │   └── synonym_dict.py           # 同义词词典管理
│   ├── chunking/                     # 文档切分模块
│   │   ├── __init__.py
│   │   ├── base_chunker.py           # 基类
│   │   ├── guideline_chunker.py      # 指南切分（按推荐意见）
│   │   ├── drug_label_chunker.py     # 药品说明书切分
│   │   ├── semantic_chunker.py       # 语义切分
│   │   └── table_chunker.py          # 表格切分
│   ├── indexing/                     # 索引构建模块
│   │   ├── __init__.py
│   │   ├── vector_index.py           # 向量索引
│   │   ├── bm25_index.py             # BM25 索引
│   │   └── graph_index.py            # 知识图谱索引
│   ├── retrievers/                   # 检索模块
│   │   ├── __init__.py
│   │   ├── bm25_retriever.py         # BM25 检索器
│   │   ├── vector_retriever.py       # 向量检索器
│   │   ├── graph_retriever.py        # GraphRAG 检索器
│   │   ├── faq_retriever.py          # FAQ 匹配检索器
│   │   ├── hybrid_retriever.py       # 混合检索器
│   │   └── reranker.py               # 重排序器
│   ├── query/                        # 查询处理模块
│   │   ├── __init__.py
│   │   ├── query_preprocessor.py     # 查询预处理（标准化/分类）
│   │   ├── query_transform.py        # 查询改写（扩展/HyDE）
│   │   └── query_decompose.py        # 查询分解
│   ├── generation/                   # 答案生成模块
│   │   ├── __init__.py
│   │   ├── generator.py              # 生成器
│   │   ├── context_compressor.py     # 上下文压缩
│   │   ├── safety_checker.py         # 安全审核模块
│   │   └── postprocessor.py          # 后处理
│   ├── evaluation/                   # 评估模块
│   │   ├── __init__.py
│   │   ├── metrics.py                # 评估指标（含幻觉率）
│   │   ├── evaluator.py              # 评估器
│   │   ├── safety_benchmark.py       # 安全评估基准
│   │   └── benchmark.py              # 基准测试
│   └── api/                          # API 接口
│       ├── __init__.py
│       └── main.py                   # FastAPI 入口
├── frontend/                         # 前端
│   └── app.py                        # Streamlit/Gradio 应用
├── scripts/                          # 工具脚本
│   ├── build_knowledge_base.py       # 构建知识库
│   ├── build_medical_graph.py        # 构建医学知识图谱
│   ├── build_synonym_dict.py         # 构建同义词词典
│   └── evaluate.py                   # 运行评估
└── tests/                            # 测试
    ├── test_parsers.py
    ├── test_retrievers.py
    ├── test_safety_checker.py
    └── test_generator.py
```

### 7.3 文档交付清单

| 文档 | 内容要求 |
|------|---------|
| `README.md` | 项目简介、安装运行指南、架构说明、Demo 展示 |
| `docs/设计文档.md` | 系统架构设计、各模块技术方案、流程图 |
| `docs/评估报告.md` | 实验设计、对比结果、分析结论、可视化图表 |
| `docs/安全评估报告.md` | 幻觉率测试、安全合规测试结果、边界案例分析 |
| `docs/使用手册.md` | 部署指南、配置说明、API 文档 |
| `notebooks/` | 实验 Notebook（数据处理、模型对比、效果分析） |

### 7.4 演示要求

- 提供可运行的 Demo（Streamlit/Gradio 或 Web 应用）
- 录制 5 分钟项目演示视频
- 演示内容包括：
  1. 上传不同类型的医学文档（指南、药品说明书等）
  2. 展示 PDF 药物表格/医学图表的解析效果
  3. 进行临床诊疗问答，展示循证引用来源
  4. 演示药物相互作用查询（展示 GraphRAG 效果）
  5. 展示安全审核机制（幻觉检测、免责声明等）
  6. 对比展示多路检索 vs 单路检索的效果差异

---

## 八、评分标准

| 评估维度 | 权重 | 评分细则 |
|---------|------|---------|
| **文档解析** | 15% | 多格式支持(5%) + PDF药物表格/医学图表处理(5%) + 术语标准化(5%) |
| **知识库构建** | 10% | 向量索引(4%) + BM25索引(2%) + 医学知识图谱构建(4%) |
| **检索系统** | 25% | 多路召回实现(10%) + 融合策略含可信度/时效性(5%) + 重排序(5%) + 查询预处理/改写(5%) |
| **答案生成与安全** | 20% | Prompt设计(4%) + 引用溯源(4%) + 安全审核机制(6%) + 后处理与免责(3%) + 幻觉控制(3%) |
| **RAG优化** | 12% | 优化策略深度(8%) + 创新实践(4%) |
| **评估实验** | 10% | 评估数据集构建(含安全专项)(3%) + 对比实验(4%) + 分析报告(3%) |
| **工程质量** | 8% | 代码规范(2%) + 测试覆盖(3%) + 文档完整(3%) |

---

## 九、里程碑计划

| 阶段 | 时间 | 交付内容 |
|------|------|---------|
| **Phase 1** | 第1-2周 | 项目搭建、医学文档解析模块、术语标准化工具、基础切分策略 |
| **Phase 2** | 第3-4周 | 向量/BM25检索、医学FAQ模块、基础RAG管线跑通 |
| **Phase 3** | 第5-6周 | 医学知识图谱构建、GraphRAG、多路融合+重排序 |
| **Phase 4** | 第7-8周 | 安全审核模块、RAG优化、评估实验（含安全专项）、前端开发 |
| **Phase 5** | 第9-10周 | 系统联调、文档撰写、Demo录制、答辩准备 |

---

## 十、参考资料

### 论文
- Lewis et al. "Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks" (2020)
- Asai et al. "Self-RAG: Learning to Retrieve, Generate, and Critique through Self-Reflection" (2023)
- Edge et al. "Local Retrieval Augmented Generation for Knowledge Graphs" (2023)
- Ma et al. "RAPTOR: Recursive Abstractive Processing for Tree-Organized Retrieval" (2024)
- Zhang et al. "Retrieval-Augmented Generation for Large Language Models: A Survey" (2024)
- Ying et al. "HuatuoGPT, towards Taming Language Model for Medical Consultation" (2023)

### 开源项目
- LangChain: https://github.com/langchain-ai/langchain
- LlamaIndex: https://github.com/run-llama/llama_index
- RAGAS: https://github.com/explodinggradients/ragas （含幻觉检测模块）
- BGE Embedding: https://github.com/FlagOpen/FlagEmbedding
- HuatuoGPT-II: https://github.com/FreedomIntelligence/HuatuoGPT-II
- Medical-Knowledge-Graph（参考实现）

### 医学数据资源
- 中国临床指南文库：https://www.guideline.cn
- 药品说明书数据库：国家药品监督管理局
- ICD-10 编码查询：https://icd.who.int/browse10/
- PubMed 文献数据库：https://pubmed.ncbi.nlm.nih.gov/
- 药物相互作用数据库：DrugBank

### 教程
- LangChain RAG Tutorial
- LlamaIndex Building RAG Course
- Pinecone RAG From Scratch Series
- RAG 系统安全性最佳实践

---

## 十一、附录：医学 RAG 特殊考量清单

> 医疗场景 RAG 相比通用场景，需额外关注以下特殊问题：

| 类别 | 考量要点 | 实施建议 |
|------|---------|---------|
| **安全性** | 错误信息可能导致健康风险 | 多层安全审核 + 免责声明 + 就医建议 |
| **幻觉** | 编造药品/剂量是致命错误 | 严格引用约束 + 幻觉检测 + 引用验证 |
| **时效性** | 医学知识持续更新 | 优先检索最新指南，标记过期内容 |
| **权威性** | 不同来源可信度差异大 | 来源分级（指南 > 说明书 > 文献 > 科普） |
| **术语** | 同一概念有多种表达 | 术语标准化 + 大规模同义词词典 |
| **隐私** | 患者数据敏感 | 本系统不涉及患者隐私数据（基于公开知识） |
| **合规** | 医疗建议受法规约束 | 明确定位为"知识辅助"而非"诊疗建议" |
| **覆盖度** | 罕见病/新药知识不足 | 明确系统知识边界，对未知问题诚实说明 |

---

> **备注：** 本项目为教学练习项目，旨在通过医疗场景实践掌握 RAG 技术栈。系统定位为医学知识辅助工具，不替代专业医疗诊断和治疗。学生可根据自身能力选择性实现各模块：
> - **基础版**：完成模块一~四（文档解析 → 知识库 → 检索 → 生成）
> - **进阶版**：增加安全审核模块 + 术语标准化 + 多路检索优化
> - **挑战版**：探索 GraphRAG + Self-RAG + 多模态医学 RAG
